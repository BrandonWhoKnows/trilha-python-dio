conjunto_a = {1, 2, 3}
conjunto_b = {2, 3, 4}

# Me dá os elementos que não estão na intersecção entre os conjuntos
# Retorna um novo conjunto com todos os elementos que estão em A ou B, mas não em ambos
# {1, 4}

resultado = conjunto_a.symmetric_difference(conjunto_b)
resultado = conjunto_a.symmetric_difference(conjunto_b)
print(resultado) 
